export const config = {
    "dev": {
      "aws_region": process.env.AWS_REGION,
      "aws_access_key_id": process.env.AWS_ACCESS_KEY_ID,
      "aws_secret_acess_key_id": process.env.AWS_SECRET_ACCESS_KEY_ID,
      "aws_media_bucket": process.env.AWS_MEDIA_BUCKET
    }
  }